import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'zw.co.anocropadvisory.app',
  appName: 'ANO Crop Advisory',
  webDir: 'dist',
  backgroundColor: '#040a0e',
};

export default config;
